from django.apps import AppConfig

class LibrarySystemConfig(AppConfig):
    # Конфігурація Django додатку для бібліотечної системи
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'library'